<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Стиль для текста -->
    <style name="TextStyle">
        <item name="android:textColor">@color/text_default</item>
        <item name="android:textSize">@dimen/defaultTextSize</item>
        <item name="android:gravity">top</item>
    </style>

    <!-- Стиль для расположения контейнера в центре внутри родительского контейнера -->
    <style name="CenterElements">
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintHeight_percent">0.95</item>
        <item name="layout_constraintWidth_percent">0.9</item>
    </style>

</resources>